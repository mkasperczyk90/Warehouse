/** Lookup feature — read-only inquiry over stock, locations and batches (the Look up tab). */
export { LookupScreen } from './LookupScreen';
export type { LookupKind, LookupRow } from './lookup.model';
