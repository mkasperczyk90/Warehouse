# assets

Static assets for the terminal app — images, icons, fonts.

Empty for now: the screens use text glyphs from the prototypes rather than
bitmap icons. Drop binary assets here and reference them with
`require('@/assets/…')` (or `expo-font` for typefaces).
