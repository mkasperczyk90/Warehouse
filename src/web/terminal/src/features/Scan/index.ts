/** Scan feature — universal scan dispatcher (the Scan tab). */
export { ScanScreen } from './ScanScreen';
export { resolveScan } from './scan.model';
export type { ScannedKind, ScanResult } from './scan.model';
