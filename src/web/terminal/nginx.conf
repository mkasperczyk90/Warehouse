server {
    listen 80;
    server_name _;
    root /usr/share/nginx/html;
    index index.html;

    # SPA: client-side routing (expo-router, web.output "single") — unknown paths
    # fall back to the shell.
    location / {
        try_files $uri $uri/ /index.html;
    }

    # The shell and the mock worker must always revalidate so a new deploy is
    # picked up; hashed Expo assets are safe to cache hard.
    location = /index.html { add_header Cache-Control "no-cache"; }
    location = /mockServiceWorker.js { add_header Cache-Control "no-cache"; }
    location /_expo/ { add_header Cache-Control "public, max-age=31536000, immutable"; }
    location /assets/ { add_header Cache-Control "public, max-age=31536000, immutable"; }

    gzip on;
    gzip_types text/css application/javascript application/json image/svg+xml;
}
