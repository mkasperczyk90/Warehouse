{
  "name": "warehouse-admin-e2e",
  "version": "0.1.0",
  "private": true,
  "description": "playwright-bdd end-to-end tests for the Warehouse admin panel (Vite SPA).",
  "scripts": {
    "bddgen": "bddgen",
    "test": "bddgen && playwright test",
    "test:headed": "bddgen && playwright test --headed",
    "test:ui": "bddgen && playwright test --ui",
    "report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.61.1",
    "@types/node": "^26.1.0",
    "playwright-bdd": "^9.1.0"
  }
}
